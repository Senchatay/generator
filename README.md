# CLI для генерации домашних паролей

Умеет:

- Создание нового пароля:

```ruby
ruby gen.rb --new [название сервиса]
```

- Получить уже имеющийся конкретный пароль.

```ruby
ruby gen.rb --give [название сервиса]
```

- Получить список всех сервисов.

```ruby
ruby gen.rb --list
```
